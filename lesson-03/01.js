// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое
const number_1 = 5
const number_2 = 6
function calculateAverage(number_1, number_2) {
    let result = (number_1 + number_2) / 2
    console.log(result);
}
